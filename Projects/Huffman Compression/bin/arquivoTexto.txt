arthur paraiba cidrao

Para converter um valor inteiro para um caractere usando a tabela ASCII em Java, voce pode 
simplesmente fazer um cast do valor inteiro para o tipo char. A tabela ASCII mapeia numeros
inteiros a caracteres, e esse cast converte o inteiro para o caractere correspondente na tabela ASCII.
Aqui esta um exemplo simples que demonstra como fazer essa conversao.